import { StylesMap } from '~/lib/constants';

const styles: StylesMap = {
  wrapper: {
    alignItems: 'center',
    display: 'flex',
    justifyContent: 'center',
    height: '100vh',
  },
};

export default styles;
