export const NUM_BRANCHES = 3;

export const SEGMENT_RENDER_INTERVAL = 1000 / 60; // FPS for drawing the lightning bolt segments

export const BOLT_LIFESPAN = {
  MIN: 1100,
  MAX: 1500,
};

export const BOLT_NUM_SEGMENTS = {
  MIN: 12,
  MAX: 20,
};

export const BOLT_TIMEOUT = {
  MIN: 2000,
  MAX: 5000,
};

export const BOLT_START_WIDTH = 5;
