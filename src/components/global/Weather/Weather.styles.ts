import { StylesMap } from '~/lib/constants';

export const styles: StylesMap = {
  canvas: {
    position: 'absolute',
  },
  container: {
    height: '100%',
    pointerEvents: 'none',
    position: 'relative',
    width: '100%',
  },
};
