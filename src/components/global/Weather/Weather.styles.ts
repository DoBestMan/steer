import { css } from '@emotion/core';

export const styles = {
  canvas: css({
    position: 'absolute',
  }),
  container: css({
    height: '100%',
    pointerEvents: 'none',
    position: 'relative',
    width: '100%',
  }),
};
