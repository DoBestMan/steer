import { StylesMap } from '~/lib/constants';

export const styles: StylesMap = {
  container: {
    position: 'relative',
  },
  frontStars: {
    left: 0,
    overflow: 'hidden',
    position: 'absolute',
    top: 0,
  },
};
