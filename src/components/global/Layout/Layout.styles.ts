import { css } from '@emotion/core';

const styles = {
  SVGSpriteContainer: css({
    display: 'none',
  }),
  container: css({
    width: '100vw',
  }),
  mainContent: css({
    height: '100%',
    minHeight: 700,
  }),
};

export default styles;
