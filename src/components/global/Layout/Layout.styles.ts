import { StylesMap } from '~/lib/constants';

const styles: StylesMap = {
  SVGSpriteContainer: {
    display: 'none',
  },
  container: {
    width: '100%',
  },
  mainContent: {
    height: '100%',
  },
};

export default styles;
