import { css } from '@emotion/core';

const styles = {
  SVGSpriteContainer: css({
    display: 'none',
  }),
  container: css({
    width: '100%',
  }),
  mainContent: css({
    height: '100%',
  }),
};

export default styles;
