import { css } from '@emotion/core';

export const styles = {
  container: css({
    '.SVGInline': {
      display: 'block',
      fontSize: 0,
      height: '100%',
    },

    display: 'inline-block',

    svg: {
      height: 'auto',
      width: '100%',
    },
  }),
};
