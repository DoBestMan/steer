import styles from './FooterMailingList.styles';

function FooterMailingList() {
  return (
    <div css={styles.container}>
      <p css={styles.heading}>Join our mailing list</p>
      <p css={styles.text}>Subscribe for updates and promotions.</p>
    </div>
  );
}

export default FooterMailingList;
