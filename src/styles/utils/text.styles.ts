import { css } from '@emotion/core';

export const textAlignCenter = css({
  textAlign: 'center',
});

export const textAlignLeft = css({
  textAlign: 'left',
});

export const textAlignRight = css({
  textAlign: 'right',
});

export const textBold = css({
  fontWeight: 'bold',
});

export const textBoldItalic = css({
  fontStyle: 'italic',
  fontWeight: 'bold',
});
