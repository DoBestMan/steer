/**
 * Content information for showing a static modal.
 * @export
 * @interface ProductCount
 */
export interface ProductCount {
  /**
   *
   * @type {string}
   * @memberof ProductCount
   */
  productCount: number | null;
}
