export enum LinkIconPosition {
  LEFT = 'left',
  RIGHT = 'right',
}

export enum LinkSize {
  REG = 'regular',
  SM = 'small',
}

export enum LinkTheme {
  DARK = 'dark',
  LIGHT = 'light',
}

export enum LinkWeight {
  BOLD = 'bold',
  NORMAL = 'normal',
}
